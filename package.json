{
  "name": "kaarigar-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo no-build-root",
    "dev": "echo use-service-level-dev",
    "lint": "npm run -ws lint",
    "test": "npm run -ws test"
  },
  "dependencies": {
    "deepmerge-ts": "^7.1.5",
    "ecdsa-sig-formatter": "^1.0.11",
    "effect": "^3.19.19"
  }
}
